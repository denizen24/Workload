{
  "name": "workload-board",
  "private": true,
  "workspaces": [
    "apps/backend",
    "apps/frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm --workspace apps/backend run start:dev\" \"npm --workspace apps/frontend run dev\"",
    "build": "npm --workspace apps/backend run build && npm --workspace apps/frontend run build",
    "start": "npm --workspace apps/backend run start:prod",
    "smoke:auth-snapshots": "node scripts/smoke-auth-snapshots.mjs",
    "smoke:compose:auth-snapshots": "node scripts/run-smoke-compose.mjs",
    "set-password": "npm --workspace apps/backend run set-password --"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
